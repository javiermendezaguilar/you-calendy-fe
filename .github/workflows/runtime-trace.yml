name: Runtime Trace

on:
  workflow_dispatch:
    inputs:
      base_url:
        description: "URL frontend a trazar"
        required: false
        default: "https://staging.groomnest.com"
        type: string
      api_url:
        description: "URL backend usada por el frontend"
        required: false
        default: "https://api.groomnest.com"
        type: string
      label:
        description: "Etiqueta del entorno"
        required: false
        default: "staging-trace"
        type: string

jobs:
  runtime-trace:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Capture authenticated runtime trace
        run: npm run perf:trace
        env:
          RUNTIME_BASE_URL: ${{ inputs.base_url }}
          RUNTIME_API_URL: ${{ inputs.api_url }}
          SMOKE_BARBER_EMAIL: ${{ secrets.SMOKE_BARBER_EMAIL }}
          SMOKE_BARBER_PASSWORD: ${{ secrets.SMOKE_BARBER_PASSWORD }}
          SMOKE_VERCEL_PROTECTION_BYPASS: ${{ secrets.SMOKE_VERCEL_PROTECTION_BYPASS }}

      - name: Upload runtime trace artifacts
        uses: actions/upload-artifact@v4
        with:
          name: runtime-trace-${{ inputs.label }}
          path: artifacts/runtime-trace/
          if-no-files-found: error
